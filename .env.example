# Mapbox Dashboard → Account → Access tokens → default or custom public token
VITE_MAPBOX_ACCESS_TOKEN=pk.eyJ1IjoiLi4uIn0...

# Optional: Geocoder dropdown label language(s), comma-separated (e.g. en | ka | en,ka). Not the keyboard language.
# VITE_GEOCODER_LANGUAGE=en,ka
